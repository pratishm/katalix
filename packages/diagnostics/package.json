{
  "name": "@lattix/diagnostics",
  "version": "0.1.0",
  "description": "Structured diagnostics, tree introspection, and debug utilities for Lattix",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@lattix/core": "0.1.0",
    "@lattix/tokens": "0.1.0"
  },
  "devDependencies": {
    "@lattix/dsl": "0.1.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
